import type { Config } from 'prettier';

const config: Config = {
  bracketSameLine: false,
  trailingComma: 'none',
  bracketSpacing: true,
  proseWrap: 'always',
  singleQuote: true,
  printWidth: 150,
  useTabs: false,
  tabWidth: 2,
  semi: true
};

export default config;
